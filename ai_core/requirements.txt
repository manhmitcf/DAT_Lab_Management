
# Jetson Requirements (L4T r36.4.0)
# Note: torch, torchvision, opencv-python are pre-installed in L4T base image or handled via apt
# numpy version is not pinned to support Python 3.12
numpy
loguru
scikit-image
tqdm
Pillow
thop
ninja
tabulate
tensorboard
lap
# motmetrics # now we use the local and customized version instead
filterpy
h5py
protobuf==3.20.3

# Release version constraints for ARM64 compatibility
onnx
onnxruntime
onnx-simplifier

# Backend & Settings synchronization
pydantic>=2.5.0
python-dotenv
websocket-client
