# Low-level parameters for the OCSort algorithm library

# Maximum number of frames to keep a track alive without new detections.
max_age=5

# Minimum number of hits to start a track.
min_hits=3

# IOU threshold for association.
iou_threshold=0.3

# === START OF FIX ===
# Kalman filter motion inertia parameter.
# A lower value (e.g., 0.1) makes the tracker trust the new detection more,
# reducing the "ghosting" or "drifting" effect of the smoothed box.
# A higher value makes the track smoother but lag more.
inertia=0.1
# === END OF FIX ===
