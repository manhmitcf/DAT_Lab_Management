[property]
gpu-id=0
net-scale-factor=1.0

# Set interval to 0 to run detection on every frame. This is the first step to remove "ghosts".
interval=0

# Force nvinfer to maintain the aspect ratio, matching the original Python preprocessing.
symmetric-padding=1

# 1. Define input size and color for TensorRT
offsets=123.675;116.28;103.53
net-scale-factor=0.0171247538316637

# 2. TensorRT engine file
model-engine-file=../../pretrained/ocsort_x_mot20_fp16.engine
onnx-file=../../pretrained/yolox.onnx

# 3. Architecture parameters
infer-dims=3;640;640
network-type=0
num-detected-classes=1
workspace-size=6144

# 4. Point to the recently compiled C++ Custom Parser library
parse-bbox-func-name=NvDsInferParseYoloX
custom-lib-path=/workspace/ai_core/deploy/DeepStream/libnvdsinfer_custom_impl_YoloX.so

# 5. Settings related to BBox filtering
[class-attrs-all]
pre-cluster-threshold=0.3
nms-iou-threshold=0.45

# === START OF FINAL FIX ===
# Disable drawing of raw detection boxes from nvinfer.
# We will let nvosd handle all drawing based on the final, smoothed tracker output.
# This ensures only ONE consistent bounding box is displayed, removing the visual "ghost" effect.
display-bbox=0
# === END OF FINAL FIX ===
